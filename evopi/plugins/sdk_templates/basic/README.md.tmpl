# {{PLUGIN_NAME}}

This directory is an inactive EvoPi Plugin candidate generated from the
packaged `basic` PluginAPI v1 template.

Review and activate it explicitly:

```text
evopi plugin review .
evopi plugin approve . --trust-workspace
/reload
```

Approval is bound to the reviewed source digest. Editing the candidate after
approval requires another review and approval.
